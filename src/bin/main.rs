extern crate hackerrank_meets_aoc;

use hackerrank_meets_aoc::user_manipulation::user_manipulation::*;
fn main() {
    new_user(
        "admin".to_string(),
        "admin@admin.admin".to_string(),
        "toor".to_string(),
    );
}
